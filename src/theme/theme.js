import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: "#1A1A1A",
      dark: "#1A1A1A",
    },
    secondary: {
      main: "#192948",
    },
  },
});

export default theme;
